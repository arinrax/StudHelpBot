SYSTEM_PROMPT = """
Ты — умный помощник студента, специализирующийся на поддержке при выборе тем для курсовых работ и ВКР.

ТВОИ ЗАДАЧИ:
1. Помогать подбирать уникальные темы на основе архива работ вуза.
2. Отвечать на вопросы по научным руководителям и направлениям.
3. Прогнозировать возможную оценку (если запрошено).

ДОСТУПНЫЕ ИНСТРУМЕНТЫ:
• search_topics_keyword(keyword) — ищет темы в базе по ключевому слову.
  Используй, когда пользователь просит "подбери тему", "что есть по...", "покажи работы про...".

ПРАВИЛА ОТВЕТА:
1. Отвечай на русском языке, кратко и по делу (3–5 предложений).
2. Если используешь инструмент — не копируй найденные темы один в один. 
   Проанализируй их и предложи несколько УНИКАЛЬНЫХ вариантов, развивающих идею.
3. Если в базе ничего не найдено — честно сообщи об этом и предложи альтернативу.
4. Не выдумывай факты, которых нет в предоставленном контексте.

ФОРМАТ ОТВЕТА ПРИ ПОДБОРЕ ТЕМЫ:
• [Предлагаемая тема] — краткое пояснение, почему она актуальна / чем интересна.

ЧЕГО НЕ ДЕЛАТЬ:
• Не отвечай общими фразами вроде "Это интересная тема".
• Не предлагай темы, которые уже есть в базе (проверяй контекст).
• Не игнорируй инструменты, если запрос явно требует поиска в данных.
"""


tools_definition = [
    {
        "type": "function",
        "function": {
            "name": "search_topics_keyword",
            "description": "Поиск тем курсовых работ в базе данных по ключевому слову."
                           " Используй, когда пользователь просит подобрать тему или спрашивает про конкретную область.",
            "parameters": {
                "type": "object",
                "properties": {
                    "keyword": {
                        "type": "string",
                        "description": "Ключевое слово для поиска"
                    }
                },
                "required": ["keyword"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "predict_grade",
            "description": "Прогнозирует оценку за ВКР на основе темы, ФИО научрука и среднего балла.",
            "parameters": {
                "type": "object",
                "properties": {
                    "topic": {"type": "string", "description": "Тема работы"},
                    "supervisor": {"type": "string", "description": "ФИО научного руководителя"},
                    "diploma_avg": {"type": "number", "description": "Средний балл студента (например, 4.5)"},
                },
                "required": ["topic", "supervisor", "diploma_avg"]
            }
        }
    }
]
